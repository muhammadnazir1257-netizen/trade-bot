# Runtime dependencies for the trading agent system.
# Pinned to known-good ranges; bump deliberately.
#
# NOTE on intentional omissions from the v2 spec:
#   - alpaca-trade-api is deprecated; we call the Alpaca REST API directly
#     with `requests`, which is version-stable and dependency-light.
#   - asyncio is a Python standard-library module; no pip package required.

requests>=2.31,<3.0
python-dotenv>=1.0,<2.0
sendgrid>=6.11,<7.0
numpy>=1.26,<3.0
pytz>=2024.1

# tzdata is only required on Windows for zoneinfo("America/New_York") to resolve.
tzdata>=2024.1; sys_platform == "win32"
